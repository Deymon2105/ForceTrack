package com.example.forcetrack.network.api

/**
 * ⚠️ ARCHIVO OBSOLETO - NO USAR
 *
 * Esta API ha sido reemplazada por XanoApi.kt
 *
 * ForceTrackApi era para una API REST genérica.
 * XanoApi está optimizada específicamente para Xano con 9 grupos de endpoints.
 *
 * RemoteRepository ahora usa XanoRepository internamente.
 *
 * Este archivo se mantiene solo por compatibilidad temporal.
 * Será eliminado en futuras versiones.
 */

// Reemplazado por: com.example.forcetrack.network.api.XanoApi
// Usa: RetrofitClient.authApi, RetrofitClient.bloqueApi, etc.
